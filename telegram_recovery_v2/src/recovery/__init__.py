"""Telegram Recovery v2 — lossless recovery/reconstruction engine.

One engine (``recovery.engine.TelegramRecoveryEngine``) is used by the CLI,
the test suite, and (later) the web application. There is no separate test
importer, CLI importer, or production importer.
"""

__version__ = "0.1.0"